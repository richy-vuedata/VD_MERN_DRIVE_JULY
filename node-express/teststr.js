// Write a function to find and return the characters that occur more than once in a given string.
// For example:
// Input: "hello"
// Output: ["l"] — because "l" appears twice


function stringCount()
{
    let input = "suresh"
    let arr = input.toArray();
    console.log(input)
}
    console.log("1234")

stringCount()